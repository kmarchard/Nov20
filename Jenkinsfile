pipeline {
    agent any
      
    stages {
        stage('Build') {
            steps {
                sh 'mvn -B -DskipTests clean package'
            }
        }
    }
   stages {
        stage('build') {
            steps {
                sh 'mvn --version'
            }
        }
        }    
        }
